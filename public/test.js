const div = document.createElement("div");

// div.innerHTML 
div.id = "Did";
div.textContent = "hhe"

const root = document.getElementById('root');

root.addEventListener

console.log(root);
root.append(div)